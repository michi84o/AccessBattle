<UserControl x:Class="AccessBattle.Wpf.View.BoardFieldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AccessBattle.Wpf.View"
             mc:Ignorable="d" 
             d:DesignHeight="64" d:DesignWidth="64">
    <Grid>
        <Border CornerRadius="10" Background="DarkOrange">

        </Border>
        <Border CornerRadius="10" Background="White" Opacity="{Binding Source={x:Static local:UiGlobals.Instance}, Path=FlashOpacity}">
           
        </Border>
        <Border CornerRadius="10" >
            <TextBlock Text="Main Content" FontSize="8" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
